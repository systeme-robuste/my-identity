/**
 * Drizzle relations. Imported by the client.
 *
 * Full relation definitions will be added in Phase 1 once the schemas are
 * finalized. For now, this is a placeholder.
 */

import { relations } from "drizzle-orm";

export const placeholder = relations; // re-export so consumers can `import { relations }`
