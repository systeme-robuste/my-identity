# @my-identity/ui

Shared React components. Used by `apps/dashboard` and `apps/marketing`.

## Status

🚧 **Initial scaffold only.** Components are stubbed. Will be filled in Phase 1 — MVP.

## Components

- `Button` — primary, secondary, ghost variants
- `Card` — basic card with border + radius
- `Input` — text input
- `Badge` — small label
- `Modal` — dialog (Radix-based, planned)
